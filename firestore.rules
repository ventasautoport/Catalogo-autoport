rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /site/catalog {
      allow read: if true;
      allow write: if request.auth != null
        && request.auth.token.email == "TU_CORREO_ADMIN@EJEMPLO.COM";
    }
  }
}